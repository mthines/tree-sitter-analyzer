# Local development artifacts — do not commit
threads/
REDESIGN_PROPOSAL.md
node_modules/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
_pytest_git_repos/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
# Local autonomous-runtime state (loop log, lock, last-tick snapshot, etc.).
# The whole directory is developer-local runtime state, EXCEPT for the
# shell scripts (status.sh, tick.sh, loop.sh) which are referenced by
# tests/unit/runtime/test_autonomous_status_script.py and must remain
# version-controlled.
.autonomous-runtime/*
!.autonomous-runtime/status.sh
!.autonomous-runtime/tick.sh
!.autonomous-runtime/loop.sh

# Workspace summary scratch file (regenerated by `--project-summary`).
summary.json

# Local analyzer/runtime state regenerated by dogfood runs.
.ast-cache/
**/.ast-cache/
.omm/
/results/

# Benchmark result payloads are local run artifacts; keep directory placeholders.
benchmarks/codegraph_compare/results/*
!benchmarks/codegraph_compare/results/.gitkeep

# Local autonomous-dev helper scripts — never commit (use opencode/CLI flags that
# bypass safety gates like --dangerously-skip-permissions and --no-verify).
# The safe, committed equivalent lives in scripts/auto_review.py.
autonomous_sprint_loop.sh
autonomous_*.sh

# Internal docs (audit findings, narrative reports, promotion strategy).
# Stays out of the public repo so the user-facing surface is the README,
# CHANGELOG, and RELEASE_NOTES — not the internal-quality-tracking
# paperwork. Engineering decisions remain visible via commit history
# and AUTONOMOUS_DEV.md (which IS public, because it documents how the
# project is run).
docs/internal/

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
#   in version control.
#   https://pdm.fming.dev/#use-with-ide
.pdm.toml

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be added to the global gitignore or merged into this project gitignore.  For a PyCharm
#  project, it is recommended to use the following settings for the Project Structure:
#  File | Settings | Project Structure | Add Content Root | Exclude files | *.pyc
.idea/

# VS Code
.vscode/
*.code-workspace

# Local History for Visual Studio Code
.history/

# Built Visual Studio Code Extensions
*.vsix

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two \r
Icon

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db

# Dump file
*.stackdump

# Folder config file
[Dd]esktop.ini

# Recycle Bin used on file shares
$RECYCLE.BIN/

# Windows Installer files
*.cab
*.msi
*.msix
*.msm
*.msp

# Windows shortcuts
*.lnk

# Linux
*~

# temporary files which can be created if a process still has a handle open of a deleted file
.fuse_hidden*

# KDE directory preferences
.directory

# Linux trash folder which might appear on any partition or disk
.Trash-*

# .nfs files are created when an open file is removed but is still being accessed
.nfs*

# Project specific files
error.log
*.log
input_handler.py
prompt_config.json
user_input_prompt.md
ISSUE_TRACKING.md
OPTIMIZATION_REPORT.md


# Development and temporary files
prompt_history/
temp/
tmp/
.temp/
.codex/
tests/temp_cli_test/

# Speckit temporary files and analysis
.specify/
# But keep important speckit configuration files
!.specify/memory/
!.specify/scripts/
!.specify/templates/

# Tree-sitter specific
tree-sitter-*/
*.so
*.dylib
*.dll

# UV lock file (keep uv.lock for reproducible builds)
# uv.lock

# Benchmark and performance files
benchmark_results/
performance_data/
.benchmark-repos/
benchmarks/codegraph_compare/results/raw/
benchmarks/codegraph_compare/results/runs.jsonl
benchmarks/codegraph_compare/results/evals.jsonl
benchmarks/codegraph_compare/results/summary.md
benchmarks/codegraph_compare/results/summary.csv
benchmarks/codegraph_compare/results/prepared_repos.json

# Local configuration files
local_config.py
local_settings.py
.local/

# Editor backup files
*~
*.swp
*.swo
*#
.#*

# Compiled tree-sitter parsers
parsers/

# TSA dogfood index cache (local, regenerated on demand)
.tree-sitter-cache/

# Compatibility test results (keep directory structure, ignore content)
compatibility_test/results/*/
compatibility_test/reports/*.md
!compatibility_test/results/.gitkeep
!compatibility_test/reports/.gitkeep

# Normalized comparison files
.analysis-*-normalized/


# Integration test reports
phase7_integration_report.json

# Format testing results
comprehensive_test_results/

# Golden masters are version controlled for regression testing
# tests/golden_masters/ is tracked
# But exclude incorrectly named golden master files
tests/golden_masters/*/golden_master_python_*.md
tests/golden_masters/*/golden_master_python_*.csv

# Temporary test files at root (should be moved to tests/)
/test*.md
/test*.js
/test*.ts
/test_class.py
/debug*.py
/sample_json.json

# Debug artifacts
tests/debug_artifacts/

# Generated TOON outputs at repo root (local artifacts)
/output.toon
/test_mcp_output.toon
/test_mcp_suppress_output.toon

# Internal design documents (not for public)
docs/designs/codexray-*.md

# gstack compiled binaries
.claude/skills/gstack/bin/*.exe

# Claude local settings
.claude/settings.local.json

# AI assistant skill cache
.claude/
.agents/

# Abandoned AI-tool configs (Cursor / Roo Code / Kiro / OpenSpec — no longer used,
# removed from the repo in the doc-system cleanup; keep them from creeping back in)
.cursor/
.cursorrules
.roo/
.roomodes
.kiro/
openspec/
.windsurf/
.aider*
.continue/

# Ruflo runtime
.claude-flow/
.swarm/
.claude/memory.db
.claude/helpers/
ruvector.db
.query_cache.json
coverage.json

# Test purge reports (Phase 1)
/reports/

# PM/agent workspace (recon / review / architect notes — not source)
.recon/

# Understand-Anything dashboard artifacts (UA writes knowledge-graph.json + intermediate/ + tmp/)
.understand-anything/
**/.understand-anything/
.mycelium/
.gstack/

# mutmut mutation testing artifacts (RFC-0017) — per-run cache, NEVER commit.
# Only tests/effectiveness/BASELINE.md (curated summary) is tracked.
.mutmut-cache
mutants/
html/mutmut/

# Local validation / benchmark / dogfood artifacts — NEVER commit
.codegraph/
.rhizome/
agentdb.rvf
agentdb.rvf.lock
/elasticsearch/
/benchmark-repos/
