[build-system]
requires = ["hatchling>=1.27,<1.31"]
build-backend = "hatchling.build"

[project]
name = "codexray"
version = "1.29.0"
description = "MCP code-intelligence server for AI agents — a more complete and more correct call graph with classified, cross-file edges. 8 facade MCP tools, 13 curated skills, TOON-compressed output, 100% local."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    { name = "aisheng.yu", email = "aimasteracc@gmail.com" },
]
maintainers = [
    { name = "Mads Thines", email = "madsthines@gmail.com" },
    { name = "aisheng.yu", email = "aimasteracc@gmail.com" },
]
keywords = [
    "tree-sitter",
    "code-analysis",
    "ast",
    "parsing",
    "static-analysis",
    "multi-language",
    "html",
    "css",
    "web-development",
    "mcp",
    "mcp-server",
    "model-context-protocol",
    "ai-tools"
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Code Generators",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Software Development :: Quality Assurance",
    "Topic :: Text Processing :: Linguistic",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Communications",
    "Framework :: AsyncIO",
    "Operating System :: OS Independent",
    "Typing :: Typed",
]

# Core dependencies (minimal set for basic functionality)
# Includes HTML/CSS support for comprehensive web technology analysis
dependencies = [
    "tree-sitter>=0.25.0",
    "chardet>=7.4.3",
    "cachetools>=5.0.0",
    "tree-sitter-java>=0.23.5",
    "tree-sitter-c>=0.20.0",
    "tree-sitter-cpp>=0.23.4",
    "tree-sitter-python>=0.23.6",
    "mcp>=1.12.3,<2.0.0",
    "tree-sitter-javascript>=0.23.1",
    "tree-sitter-markdown>=0.3.1",
    "tree-sitter-html>=0.23.0",
    "tree-sitter-css>=0.23.0",
    "psutil>=5.9.8",
    "tree-sitter-typescript>=0.23.2",
    "deepdiff>=6.7.1",
    "jsonschema>=4.0.0",
    "tree-sitter-c-sharp>=0.23.1",
    "tree-sitter-sql>=0.3.11",
    "tree-sitter-php>=0.24.1",
    "tree-sitter-ruby>=0.23.1",
    "tree-sitter-yaml>=0.7.0",
    "tree-sitter-go>=0.20.0",
    "tree-sitter-rust>=0.20.0",
    "tree-sitter-kotlin>=0.3.0",
    "detect-secrets>=1.5.0",
    "tree-sitter-json>=0.23.0",
    "networkx>=3.4.2",
    "numpy>=2.2.6",
    "pyyaml>=6.0",
    "pathspec>=0.12.1",
    "anthropic>=0.104.1",
    "tree-sitter-bash>=0.23.0",
    "tree-sitter-scala>=0.23.0",
]

# Add C# to optional dependencies
[project.optional-dependencies]
# Core language support - install specific languages as needed
java = ["tree-sitter-java>=0.23.5,<0.25.0"]
javascript = [
    "tree-sitter-javascript>=0.23.1,<0.25.0",
]
typescript = [
    "tree-sitter-typescript>=0.20.0,<0.25.0",
]
python = [
    "tree-sitter-python>=0.23.0,<0.25.0",
]
c = ["tree-sitter-c>=0.20.0,<0.25.0"]
cpp = ["tree-sitter-cpp>=0.23.4,<0.25.0"]
rust = ["tree-sitter-rust>=0.20.0,<0.25.0"]
go = ["tree-sitter-go>=0.20.0,<0.25.0"]
markdown = ["tree-sitter-markdown>=0.3.1"]
graph = ["ladybug>=0.17.1,<0.18.0"]
html = ["tree-sitter-html>=0.23.0,<0.25.0"]  # HTML DOM analysis with element classification
css = ["tree-sitter-css>=0.23.0,<0.25.0"]   # CSS selector and property analysis with specificity calculation
sql = ["tree-sitter-sql>=0.3.11,<0.4.0"]   # SQL language support for database schema analysis
csharp = ["tree-sitter-c-sharp>=0.23.1"]  # C# language support for .NET applications
php = ["tree-sitter-php>=0.23.0,<0.25.0"]  # PHP language support for web applications
ruby = ["tree-sitter-ruby>=0.23.0,<0.25.0"]  # Ruby language support for Rails and scripting
yaml = ["tree-sitter-yaml>=0.7.0"]  # YAML language support for configuration files
kotlin = [
    "tree-sitter-kotlin>=0.3.0",
]  # Kotlin language support for Android and JVM applications
swift = [
    "tree-sitter-swift>=0.7.2",
]  # Swift language support for Apple platform applications

# Language bundles for convenience
web = [
    "tree-sitter-javascript>=0.23.1,<0.25.0",
    "tree-sitter-typescript>=0.20.0,<0.25.0",
    "tree-sitter-html>=0.23.0,<0.25.0",      # Complete HTML5 support with semantic analysis
    "tree-sitter-css>=0.23.0,<0.25.0",       # Complete CSS3 support with advanced property categorization
    "tree-sitter-php>=0.23.0,<0.25.0",       # PHP for WordPress, Laravel, Symfony
    "tree-sitter-ruby>=0.23.0,<0.25.0"       # Ruby for Rails applications
]
systems = [
    "tree-sitter-c>=0.20.0,<0.25.0",
    "tree-sitter-cpp>=0.23.4,<0.25.0",
    "tree-sitter-rust>=0.20.0,<0.25.0",
    "tree-sitter-go>=0.20.0,<0.25.0"
]
popular = [
    "tree-sitter-java>=0.23.5,<0.25.0",
    "tree-sitter-python>=0.23.0,<0.25.0",
    "tree-sitter-javascript>=0.23.1,<0.25.0",
    "tree-sitter-typescript>=0.20.0,<0.25.0"
]
all-languages = [
    "tree-sitter-java>=0.23.5,<0.25.0",
    "tree-sitter-javascript>=0.23.1,<0.25.0",
    "tree-sitter-typescript>=0.20.0,<0.25.0",
    "tree-sitter-python>=0.23.0,<0.25.0",
    "tree-sitter-c>=0.20.0,<0.25.0",
    "tree-sitter-cpp>=0.23.4,<0.25.0",
    "tree-sitter-rust>=0.20.0,<0.25.0",
    "tree-sitter-go>=0.20.0,<0.25.0",
    "tree-sitter-kotlin>=0.3.0",  # Kotlin language support for Android and JVM applications
    "tree-sitter-swift>=0.7.2",  # Swift language support for Apple platform applications
    "tree-sitter-markdown>=0.3.1",
    "tree-sitter-html>=0.23.0,<0.25.0",
    "tree-sitter-css>=0.23.0,<0.25.0",
    "tree-sitter-sql>=0.3.11,<0.4.0",  # SQL language support for database schema analysis
    "tree-sitter-c-sharp>=0.23.1",  # C# language support for .NET applications
    "tree-sitter-php>=0.23.0,<0.25.0",  # PHP language support for web applications
    "tree-sitter-ruby>=0.23.0,<0.25.0",  # Ruby language support for Rails and scripting
    "tree-sitter-yaml>=0.7.0",  # YAML language support for configuration files
    "tree-sitter-bash>=0.23.0,<0.26.0",  # Bash/shell script support
    "tree-sitter-scala>=0.23.0,<0.27.0",  # Scala language support
]

# MCP server support
mcp = [
    "mcp>=1.12.2",
    "anyio>=4.0.0",
    "httpx>=0.27.0,<1.0.0",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.2.1",
]

# Development dependencies
dev = [
    "pytest>=8.4.1",
    "pytest-cov>=4.0.0",
    "pytest-asyncio>=1.4.0",
    "pytest-mock>=3.14.1",
    "pytest-xdist>=3.8.0",
    "pytest-timeout>=2.4.0",
    "pytest-rerunfailures>=16.3",
    "hypothesis>=6.0.0",
    "black>=24.0.0",
    "ruff>=0.5.0",
    "mypy>=1.17.0",
    "isort>=8.0.1",
    "types-psutil>=5.9.0",
    "memory-profiler>=0.61.0",
    "psutil>=5.9.6,<6",
    "pre-commit>=3.0.0",
]

# Testing with comprehensive language support
test = [
    "pytest>=8.4.1",
    "pytest-cov>=4.0.0",
    "pytest-asyncio>=1.4.0",
    "pytest-mock>=3.14.1",
    "pytest-xdist>=3.8.0",
    "pytest-timeout>=2.4.0",
    "pytest-rerunfailures>=16.3",
    "tree-sitter-java>=0.23.5",
    "tree-sitter-python>=0.23.0",
    "tree-sitter-javascript>=0.23.1",
    "tree-sitter-cpp>=0.23.4",
    "tree-sitter-sql>=0.3.11,<0.4.0",
    "tree-sitter-php>=0.23.0,<0.25.0",
    "tree-sitter-ruby>=0.23.0,<0.25.0",
    "tree-sitter-c-sharp>=0.23.1",
]

# Integration testing dependencies (Phase 7)
integration = [
    "pytest>=8.4.1",
    "pytest-cov>=4.0.0",
    "pytest-asyncio>=1.4.0",
    "pytest-mock>=3.14.1",
    "pytest-xdist>=3.8.0",
    "pytest-timeout>=2.4.0",
    "pytest-benchmark>=4.0.0",
    "psutil>=5.9.8",
    "memory-profiler>=0.61.0",
    "tree-sitter-java>=0.23.5",
    "tree-sitter-python>=0.23.0",
    "tree-sitter-javascript>=0.23.1",
    "tree-sitter-typescript>=0.20.0",
    "tree-sitter-markdown>=0.3.1",
    "mcp>=1.12.2",
    "anyio>=4.0.0",
]

# Complete development environment
full = [
    "tree-sitter-java>=0.23.5,<0.25.0",
    "tree-sitter-javascript>=0.23.1,<0.25.0",
    "tree-sitter-typescript>=0.20.0,<0.25.0",
    "tree-sitter-python>=0.23.0,<0.25.0",
    "tree-sitter-c>=0.20.0,<0.25.0",
    "tree-sitter-cpp>=0.23.4,<0.25.0",
    "tree-sitter-rust>=0.20.0,<0.25.0",
    "tree-sitter-go>=0.20.0,<0.25.0",
    "tree-sitter-markdown>=0.3.1",
    "tree-sitter-swift>=0.7.2",
    "mcp>=1.12.2",
    "anyio>=4.0.0",
    "httpx>=0.27.0,<1.0.0",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.2.1",
    "pytest>=8.4.1",
    "pytest-cov>=4.0.0",
    "pytest-asyncio>=1.4.0",
    "pytest-mock>=3.14.1",
    "pytest-xdist>=3.8.0",
    "pytest-timeout>=2.4.0",
    "black>=24.0.0",
    "ruff>=0.5.0",
    "mypy>=1.17.0",
    "isort>=8.0.1",
    "types-psutil>=5.9.0",
    "memory-profiler>=0.61.0",
    "psutil>=5.9.6,<6",
    "pre-commit>=3.0.0",
]

# All dependencies (alias for full)
all = [
    "tree-sitter-java>=0.23.5,<0.25.0",
    "tree-sitter-javascript>=0.23.1,<0.25.0",
    "tree-sitter-typescript>=0.20.0,<0.25.0",
    "tree-sitter-python>=0.23.0,<0.25.0",
    "tree-sitter-c>=0.20.0,<0.25.0",
    "tree-sitter-cpp>=0.23.4,<0.25.0",
    "tree-sitter-rust>=0.20.0,<0.25.0",
    "tree-sitter-go>=0.20.0,<0.25.0",
    "tree-sitter-markdown>=0.3.1",
    "tree-sitter-swift>=0.7.2",
    "tree-sitter-html>=0.23.0,<0.25.0",
    "tree-sitter-css>=0.23.0,<0.25.0",
    "tree-sitter-sql>=0.3.11,<0.4.0",  # SQL language support for database schema analysis
    "mcp>=1.12.2",
    "anyio>=4.0.0",
    "httpx>=0.27.0,<1.0.0",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.2.1",
    "pytest>=8.4.1",
    "pytest-cov>=4.0.0",
    "pytest-asyncio>=1.4.0",
    "pytest-mock>=3.14.1",
    "pytest-xdist>=3.8.0",
    "pytest-timeout>=2.4.0",
    "black>=24.0.0",
    "ruff>=0.5.0",
    "mypy>=1.17.0",
    "isort>=8.0.1",
    "types-psutil>=5.9.0",
    "memory-profiler>=0.61.0",
    "psutil>=5.9.6,<6",
    "pre-commit>=3.0.0",
]
[project.scripts]
# Main CLI entry point
codexray = "codexray.cli_main:main"
# MCP server entry point
codexray-mcp = "codexray.mcp.server:main_sync"
# Doctor / diagnostics entry point
codexray-doctor = "codexray.cli_main:main_doctor"
# Mis-Wire Audit — run-on-your-repo cross-language correctness demo
miswire-audit = "codexray.miswire_audit:main"
# Legacy aliases for backward compatibility
code-analyzer = "codexray.cli_main:main"
java-analyzer = "codexray.cli_main:main"
list-files = "codexray.cli.commands.list_files_cli:main"
search-content = "codexray.cli.commands.search_content_cli:main"
find-and-grep = "codexray.cli.commands.find_and_grep_cli:main"

[project.entry-points."codexray.plugins"]
# Core language plugins (automatically discovered)
java = "codexray.languages.java_plugin:JavaPlugin"
python = "codexray.languages.python_plugin:PythonPlugin"
javascript = "codexray.languages.javascript_plugin:JavaScriptPlugin"
typescript = "codexray.languages.typescript_plugin:TypeScriptPlugin"
markdown = "codexray.languages.markdown_plugin:MarkdownPlugin"
html = "codexray.languages.html_plugin:HtmlPlugin"
css = "codexray.languages.css_plugin:CssPlugin"
csharp = "codexray.languages.csharp_plugin:CSharpPlugin"
sql = "codexray.languages.sql_plugin:SQLPlugin"
php = "codexray.languages.php_plugin:PHPPlugin"
ruby = "codexray.languages.ruby_plugin:RubyPlugin"
yaml = "codexray.languages.yaml_plugin:YAMLPlugin"
json = "codexray.languages.json_plugin:JSONPlugin"
go = "codexray.languages.go_plugin:GoPlugin"
rust = "codexray.languages.rust_plugin:RustPlugin"
kotlin = "codexray.languages.kotlin_plugin:KotlinPlugin"
c = "codexray.languages.c_plugin:CPlugin"
cpp = "codexray.languages.cpp_plugin:CppPlugin"
swift = "codexray.languages.swift_plugin:SwiftPlugin"
bash = "codexray.languages.bash_plugin:BashPlugin"
scala = "codexray.languages.scala_plugin:ScalaPlugin"

[project.urls]
Homepage = "https://github.com/mthines/codexray"
Documentation = "https://github.com/mthines/codexray#readme"
Repository = "https://github.com/mthines/codexray.git"
Issues = "https://github.com/mthines/codexray/issues"
Changelog = "https://github.com/mthines/codexray/blob/main/CHANGELOG.md"
"Bug Reports" = "https://github.com/mthines/codexray/issues"
"Source Code" = "https://github.com/mthines/codexray"
Upstream = "https://github.com/aimasteracc/tree-sitter-analyzer"

[tool.hatch.build.targets.wheel]
packages = ["codexray"]

[tool.hatch.build.targets.sdist]
exclude = [
    "/.github",
    "/tests",
    "/archived_files",
    "/.pytest_cache",
    "/.mypy_cache",
    "/.coverage",
    "/htmlcov",
    "/.claude",
]

[tool.hatch.metadata]
allow-direct-references = true

[tool.black]
line-length = 88
target-version = ['py310', 'py311', 'py312', 'py313']
include = '\.pyi?$'
extend-exclude = '''
/(
  # directories
  \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | _build
  | buck-out
  | build
  | dist
  | archived_files
)/
'''

[tool.isort]
profile = "black"
line_length = 88
multi_line_output = 3
include_trailing_comma = true
force_grid_wrap = 0
use_parentheses = true
ensure_newline_before_comments = true
known_first_party = ["codexray"]
sections = ["FUTURE", "STDLIB", "THIRDPARTY", "FIRSTPARTY", "LOCALFOLDER"]

[tool.mypy]
python_version = "3.10"
explicit_package_bases = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true
show_error_codes = true
show_column_numbers = true
pretty = true

# Exclude archived files and test files from strict checking
[[tool.mypy.overrides]]
module = [
    "tests.*",
    "archived_files.*",
]
ignore_errors = true

# Handle missing stubs for tree-sitter packages + optional watchdog dep
[[tool.mypy.overrides]]
module = [
    "tree_sitter.*",
    # ``watchdog`` is an optional dep (file_watcher's polling fallback
    # covers the no-watchdog case). Its PyPI dist ships no stubs and we
    # don't want to require types-watchdog just for one optional path.
    "watchdog.*",
    # All 21 tree-sitter language modules (for 17 languages + aliases)
    "tree_sitter_bash.*",
    "tree_sitter_c.*",
    "tree_sitter_c_sharp.*",
    "tree_sitter_cpp.*",
    "tree_sitter_css.*",
    "tree_sitter_go.*",
    "tree_sitter_html.*",
    "tree_sitter_java.*",
    "tree_sitter_javascript.*",
    "tree_sitter_json.*",
    "tree_sitter_kotlin.*",
    "tree_sitter_lua",
    "tree_sitter_lua.*",
    "tree_sitter_markdown.*",
    "tree_sitter_php.*",
    "tree_sitter_python.*",
    "tree_sitter_ruby.*",
    "tree_sitter_rust.*",
    "tree_sitter_scala.*",
    "tree_sitter_sql.*",
    "tree_sitter_swift.*",
    "tree_sitter_typescript.*",
    "tree_sitter_yaml.*",
]
ignore_missing_imports = true

# AUDIT-INFRA-1: modules that operate directly on tree-sitter Node objects
# inherit Any-typed returns and attributes because tree-sitter has no type
# stubs in this repo. We silence the four resulting noise codes
# (attr-defined / no-any-return / no-untyped-def / unreachable) on
# exactly the modules that need them, so real type errors (arg-type,
# assignment, var-annotated, misc) still surface elsewhere — and on
# clean modules these codes still fail builds. This restores pre-commit
# mypy as a useful gate, eliminating the routine need for --no-verify.
[[tool.mypy.overrides]]
module = [
    "codexray._api_query_helpers",
    "codexray._api_result_helpers",
    "codexray._encoding_detection",
    "codexray.cli.parser_readiness",
    "codexray.cli.parser_readiness_package",
    "codexray.cli.parser_readiness_records",
    "codexray.cli.parser_readiness_sources",
    "codexray.core._analysis_engine_analysis_mixin",
    "codexray.core._analysis_engine_code_mixin",
    "codexray.core._analysis_engine_file_mixin",
    "codexray.core._analysis_engine_runtime_mixin",
    "codexray.core._query_execution",
    "codexray.formatters._cpp_formatter_helpers",
    "codexray.formatters._java_formatter_class_mixin",
    "codexray.formatters._java_formatter_compact_mixin",
    "codexray.formatters._java_formatter_full_mixin",
    "codexray.formatters._javascript_formatter_rows_mixin",
    "codexray.formatters._python_formatter_compact",
    "codexray.formatters._python_formatter_full_classes",
    "codexray.formatters._python_formatter_full_functions",
    "codexray.formatters._python_formatter_rows",
    "codexray.formatters._python_formatter_signatures",
    "codexray.formatters._sql_formatter_wrapper_extractors",
    "codexray.formatters._sql_formatters_helpers",
    "codexray.formatters._typescript_formatter_helpers",
    "codexray.formatters.sql_formatters",
    "codexray.incremental_sync",
    "codexray.language_loader",
    "codexray.languages._cpp_plugin_template_helpers",
    "codexray.languages._go_common_helpers",
    "codexray.languages._go_type_helpers",
    "codexray.languages._java_ast_helpers",
    "codexray.languages._java_traversal_helpers",
    "codexray.languages._swift_plugin_nodes",
    "codexray.languages.cpp_plugin",
    "codexray.languages.json_plugin",
    "codexray.languages.javascript_plugin._function_mixin",
    "codexray.languages.javascript_plugin._import_export_mixin",
    "codexray.languages.javascript_plugin._public_extraction_mixin",
    "codexray.languages.javascript_plugin._utility_mixin",
    "codexray.languages.javascript_plugin.extractor",
    "codexray.languages.markdown_plugin._misc_inline_extractor_helpers",
    "codexray.languages.markdown_plugin.extractor",
    "codexray.languages.markdown_plugin.private_extraction",
    "codexray.languages.markdown_plugin.public_extraction",
    "codexray.languages.python_plugin._class_extractor_mixin",
    "codexray.languages.python_plugin._core_extractor_mixin",
    "codexray.languages.python_plugin._function_extractor_mixin",
    "codexray.languages.python_plugin._import_package_mixin",
    "codexray.languages.python_plugin._signature_helpers",
    "codexray.languages.sql_plugin.extractor",
    "codexray.languages.sql_plugin.plugin",
    "codexray.languages.typescript_plugin._signature_helpers",
    "codexray.languages.typescript_plugin.extractor",
    "codexray.mcp.server",
    "codexray.mcp.server_utils.tool_registration",
    "codexray.mcp.tools.ast_cache_tool",
    "codexray.mcp.tools.code_patterns_tool",
    "codexray.mcp.tools.find_and_grep_tool",
    "codexray.mcp.tools.search_content_helpers",
    "codexray.mcp.tools.search_content_response",
    "codexray.mcp.tools.search_content_response_modes",
    "codexray.mcp.tools.utils.file_health_response",
    "codexray.mcp.tools.utils.file_health_smells",
    "codexray.mcp.tools.utils.refactoring_suggestions_helpers",
    "codexray.platform_compat.profiles",
    "codexray.plugins._base_traverse_mixin",
    "codexray.plugins._manager_helpers",
    "codexray.plugins.base",
]
disable_error_code = [
    "attr-defined",
    "no-any-return",
    "no-untyped-def",
    "unreachable",
    # These also stem from Any-typed Node propagation: a Node passed to
    # any strictly-typed function trips arg-type / assignment / etc.
    # We still get real arg-type catches in modules NOT in this override
    # list, so plain Python type bugs are still gated by mypy.
    "arg-type",
    "assignment",
    "var-annotated",
    "misc",
    "call-arg",
    "return-value",
    "union-attr",
    "type-abstract",
    "override",
]
# When we silence those codes globally on these modules, any inline
# `# type: ignore[<silenced-code>]` annotation becomes "unused". Don't
# turn that into a noisy second-order failure — those annotations were
# there for a reason and remain harmless.
warn_unused_ignores = false

[tool.coverage.run]
source = ["codexray"]
omit = [
    "*/tests/*",
    "*/archived_files/*",
    "*/__pycache__/*",
    "*/.*",
]
branch = true

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if self.debug:",
    "if settings.DEBUG",
    "raise AssertionError",
    "raise NotImplementedError",
    "if 0:",
    "if __name__ == .__main__.:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
    "TYPE_CHECKING",
]
show_missing = true
skip_covered = false
precision = 2

[tool.coverage.html]
directory = "htmlcov"

# MCP server configuration
[tool.mcp]
server_name = "codexray"
server_version = "1.29.0"
description = "AI-era enterprise-grade code analysis MCP server with comprehensive HTML/CSS support and multi-language capabilities"
capabilities = [
    "check_code_scale",
    "analyze_code_structure",
    "extract_code_section",
]

[tool.ruff]
line-length = 88
target-version = "py310"
exclude = [
    "corpus/",  # Test corpus files - demonstration code, not production
    # Fixture files are deliberate sample code for testing extractors —
    # their import order, dead names, etc. are part of the test surface.
    "tests/fixtures/",
]

[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
    "UP", # pyupgrade
]
ignore = [
    "E501",  # line too long, handled by black
    "B008",  # do not perform function calls in argument defaults
    "C901",  # too complex
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B011"]
"tests/fixtures/project_graph/**/*.py" = ["F401", "I001"]
"codexray/ast_cache.py" = ["I001"]

[tool.ruff.lint.isort]
known-first-party = ["codexray"]

[tool.bandit]
exclude_dirs = ["tests", "archived_files"]
# B101 assert_used (tests-only); B601 paramiko_calls (not used);
# B404 import_subprocess, B603 subprocess_without_shell, B607 partial_path:
# project legitimately invokes ``git`` (list-form, never shell=True) — these
# are not real vulnerabilities here. See ast_diff_tool/semantic_classify_tool.
# B110 try_except_pass, B112 try_except_continue: project uses these as
# deliberate degrade-silently patterns in DB / git / parser fallbacks where
# the indexer must never crash on environmental oddities. Each call site has
# a comment explaining why; no real exception swallowing in security-relevant paths.
skips = ["B101", "B601", "B404", "B603", "B607", "B110", "B112", "B608"]
# B608 hardcoded_sql_expressions: project uses parameterized queries exclusively
# (placeholders = "?,?,?" with bind params). The f-string is used only to
# interpolate the placeholder count, never user data — false positive.

[tool.mutmut]
# Phase 1 (RFC-0017): measure mutation score for core modules only.
# Each source_path is mutated and run against a targeted test selection —
# this keeps per-mutant runtime small and the overall baseline tractable.
#
# Run:  uv run mutmut run
# See:  uv run mutmut results
#
# NOTE: mutmut relies on os.fork — run on Linux (ubuntu-latest) only; NOT Windows.
source_paths = [
    "codexray/ast_diff.py",
    "codexray/semantic_change_classifier.py",
    "codexray/mcp/tools/facade_tool.py",
    "codexray/mcp/tools/query_symbol_search.py",
    "codexray/formatters/toon_encoder.py",
]
# Targeted test files per mutated module (reduces per-mutant runtime vs full suite).
pytest_add_cli_args_test_selection = [
    "tests/unit/test_ast_diff.py",
    "tests/unit/test_ast_diff_tool.py",
    "tests/unit/test_semantic_change_classifier.py",
    "tests/unit/test_semantic_classify_tool.py",
    "tests/unit/mcp/test_output_cost_invariants.py",
]
# Disable parallel pytest inside mutmut (each mutant is already a fork;
# xdist inside would nest processes and OOM — same lesson as the 140G incident).
pytest_add_cli_args = ["-n", "0", "-x", "--timeout=30", "-q"]
# Generous timeout per mutant (tree-sitter init is slow on first import).
timeout_multiplier = 10.0
timeout_constant = 10.0

[dependency-groups]
dev = [
    "build>=1.2.2.post1",
    "pre-commit>=4.2.0",
    "pytest>=8.4.1",
    "pytest-asyncio>=1.4.0",
    "pytest-mock>=3.14.1",
    "pytest-cov>=4.0.0",
    "ruff>=0.5.0",
    "twine>=4.0.2",
    "black>=24.0.0",
    "isort>=8.0.1",
    "bandit>=1.7.0",
    "safety>=3.0.0,<3.9.0", # Pin to avoid typer compatibility issues
    "psutil>=5.9.8",
    "types-toml>=0.10.8.20240310",
    "types-requests>=2.32.4.20250913",
    "types-jsonschema",
    "types-PyYAML",
    "types-cachetools",
    "mypy==1.19.1",
    "hypothesis>=6.0.0",
    "pytest-xdist>=3.8.0",
    "pytest-timeout>=2.4.0",
    "pytest-benchmark>=5.1.0",
    "pytest-rerunfailures>=16.3",
    # Python 3.10 lacks tomllib; tests fall back via ``try/except ImportError``.
    "tomli>=2.0.0; python_version<'3.11'",
    "mutmut>=3.6.0",
]
